 Anagram_validator.py - it compares two words if they both have same charcters.
 BMI.py - it calculates BMI based on your height (cm) and weight (kg).
 BackgroundRemover.py - removes the background of jpeg file and return png file.
 BudgetTracker.py - it allows to add money, spending, monthly subscription and presents a nice balance sheet.
 CLoud_file_transfer.py - it generates a link to retrive uploaded (single)file.
 Cloud_Multi_Transfer.py - it generates a link to retrive uploaded files.
 Correct_Spell - it corrects wrong spelling into a correct spelling.
 Currency_Converter.py - it is a real-time currency converter.
 Current_location.py - it gives current location based on IP address.
 DiceRoll.py - it is a dice roll simulator.
 Emjoi_to_Text.py - it converts emoji into a text which shows the description of an emoji.
 Flashcard.py - it conerts text file in a form of flash cards.
 FtoC.py - it converts fahrenheit to celsius.
 Gif.py - combine images to create a gif out of it.
 Handwriting.py - it converts text to handwriting.
 Image_Color_Extraction.py - based on pallete count it generates the color pallete from a color used in image.
 LCM.py - it finds out least common number, which is divisible by both entered numbers.
 Lang_Detect_Translate.py - it detects the language other than english and translates into english.
 Laptop_notification.py - simple notification to remind you take break "Advanced version reminder app in python directory".
 Organizer.py - it organizes the text file in alphabatical order.
 Palindrome.py - it identifies the word that spells same from forward or backword - Palindrome Words
 Qrcode_Generator.py - generates QR code for any link/website.
 Repeat_Char_Counter.py - it counts charcter occorences in the single statement.
 Roman_to_Decimal.py - it converts roman numbers in decimal.
 Screen_Recorder.py - records the screen activity and saves in mp4 file format.
 Secure_password_generator.py - it generates a random combination of secure password.
 Speech_to_Text.py - it converts speech into text format.
 StickyNotes.py - it converts xlsx file and shows the upcoming tasks.
 StickyNotes_GUI.py - it converts xlsx file and shows the upcoming tasks in interactive user interface.
 StopWatch.py - it creates a countdown of seconds like a timer.
 VoiceRecorder.py - it records voice for duration entered via system argument
 WordClouds_bg.py - creates a background image from text, which higlights different text as cloud.
 bracode.py - create a barcode from numeric data.
 captcha_generator.py - generates captcha easy and difficult both .
 password_authenticator.py - it authenticates the entered password via user based on database entry.
 pdf_to_speech.py - it reads out content of the pdf file.
 pdf_to_word.py - converts pdf files into word.
 png_to_jpg.py - converts png file into jpeg format
 random_card.py - it selects a random card from the deck of 52 cards
 vulnerability_scanner.py - it scans url for potential IDOR, SQL Injection or CSRF vulnerabilities.
 weather.py - generates real-time weather information based on location and also provide next 5 days weather forecast.
 word_to_pdf.py - convertes word/text file in pdf format.
 Age_Calculator.py - based on the birthdate it return age.
 Selection_sort.py - it goes through charcter by charcter and swaps them based on lower and higher number.
 bubble_sort.py - it goes from bottom to top in reverse order pushes(swaps) lower charcter upwards and sorts it.
